function startVoice(id){const R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R)return;let r=new R();r.lang='fr-CA';r.start();r.onresult=e=>document.getElementById(id).value+=(e.results[0][0].transcript);}function clearForm(){document.querySelectorAll('input,textarea').forEach(x=>x.value='')}function saveLocal(){localStorage.setItem('rapport',JSON.stringify([...document.querySelectorAll('input,textarea,select')].map(x=>[x.id,x.value])));alert('Sauvegardé');}function exportExcel(){const data=[];document.querySelectorAll('input,textarea,select').forEach(x=>data.push({Champ:x.id,Valeur:x.value}));const wb=XLSX.utils.book_new();const ws=XLSX.utils.json_to_sheet(data);XLSX.utils.book_append_sheet(wb,ws,'Rapport');XLSX.writeFile(wb,'Rapport_Maintenance.xlsx');}const c=document.getElementById('sig');if(c){const ctx=c.getContext('2d');let d=false;c.onmousedown=()=>d=true;c.onmouseup=()=>d=false;c.onmousemove=e=>{if(!d)return;ctx.lineTo(e.offsetX,e.offsetY);ctx.stroke();};}
